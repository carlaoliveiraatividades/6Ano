rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir operações para as coleções da plataforma pedagógica
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

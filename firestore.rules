rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para funcionamento integral da plataforma educativa Missão TIC
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
